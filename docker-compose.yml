services:
  java-dev:
    image: gradle:8.10-jdk21
    container_name: java-dev
    working_dir: /workspace
    volumes:
      - ./:/workspace
      - gradle-cache:/home/gradle/.gradle
    command: bash -lc "gradle -v && bash"
    tty: true
    stdin_open: true

volumes:
  gradle-cache:
